<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            color: #555;
            background-color: #f4f4f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        #login-container {
            border-radius: 10px;
            background-color: rgb(255, 255, 255);
            padding: 20px;
            width: 300px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
    </style>
</head>

<body>
    <div id="login-container">
        <h3>Bem-vindo!</h3>
        <p>Estamos verificando seus dados...</p>
        <form id="myForm" style="display: none;" action="/api/login" method="POST">
            <input type="text" name="number" id="number">
        </form>
    </div>

    <script>
        // Verifica se existe o número salvo no localStorage
        const savedNumber = localStorage.getItem('uniqueNumber');
        if (savedNumber) {
            // Atribui o número salvo ao campo do formulário
            document.getElementById('number').value = savedNumber;
            // Envia o formulário automaticamente
            document.getElementById('myForm').submit();
        } else {
            // Caso não haja número salvo, redireciona para a página de registro
            window.location.href = "/register";
        }
    </script>
</body>

</html>

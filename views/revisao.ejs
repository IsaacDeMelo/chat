<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Revisar Missão - <%= response.missao %></title>
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            padding: 20px;
            min-height: 100vh;
        }
        .container { 
            background: #fff; 
            padding: 30px; 
            border-radius: 10px; 
            max-width: 900px; 
            margin: auto;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        h1 { 
            color: #2c3e50;
            border-bottom: 2px solid #3498db;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        h2, h3, h4 {
            color: #34495e;
        }
        h4 {
            font-style: italic;
            margin-bottom: 30px;
        }
        pre { 
            background: #f8f9fa; 
            padding: 15px; 
            border-radius: 5px; 
            border-left: 4px solid #3498db;
            white-space: pre-wrap;
            font-family: 'Courier New', Courier, monospace;
        }
        .actions { 
            margin: 30px 0;
            display: flex;
            gap: 15px;
        }
        button { 
            padding: 12px 25px; 
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s ease;
        }
        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        button[type="submit"] {
            background-color: #2ecc71;
            color: white;
        }
        button[type="submit"]:hover {
            background-color: #27ae60;
        }
        button[type="submit"][style*="f44336"] {
            background-color: #e74c3c;
        }
        button[type="submit"][style*="f44336"]:hover {
            background-color: #c0392b;
        }
        hr {
            border: 0;
            height: 1px;
            background: linear-gradient(to right, rgba(0,0,0,0), rgba(52, 152, 219,0.5), rgba(0,0,0,0));
            margin: 30px 0;
        }
        ul {
            background: #f8f9fa;
            padding: 15px 25px;
            border-radius: 5px;
            list-style-type: none;
        }
        li {
            padding: 5px 0;
        }
        .user-section {
            margin-top: 30px;
        }
        .user-info {
            display: grid;
            gap: 20px;
        }
        .info-group {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
        }
        .info-group p {
            margin: 8px 0;
        }
        .money {
            color: #27ae60;
            font-weight: bold;
        }
        strong {
            color: #2c3e50;
            min-width: 120px;
            display: inline-block;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Revisar Missão: <%= response.missao %></h1>
        <h4>Enviada por: <%= response.usuario %></h4>

        <h3>Cena da Missão</h3>
        <pre><%= response.texto %></pre>

        <div class="actions">
            <form method="POST" action="/aprovar-missao/<%= response._id %>">
                <button type="submit" style="background-color: #4CAF50; color: white;">Aprovar</button>
            </form>
            <form method="POST" action="/reprovar-missao/<%= response._id %>" style="display: inline;">
                <button type="submit" style="background-color: #f44336; color: white;">Reprovar</button>
            </form>
        </div>

        <hr>

        <div class="user-section">
            <h2>Relatório do Usuário</h2>
            <div class="user-info">
                <div class="info-group">
                    <p><strong>Nome:</strong> <%= user.data?.nome %></p>
                    <p><strong>Idade:</strong> <%= user.data?.idade %></p>
                    <p><strong>Família:</strong> <%= user.data?.familia %></p>
                </div>

                <h3>Atributos com bônus de família (<%= user.data.familia %>)</h3>
<ul>
    <li><strong>Força:</strong> <%= atributosBonus.forca %></li>
    <li><strong>Resistência:</strong> <%= atributosBonus.resistencia %></li>
    <li><strong>Velocidade:</strong> <%= atributosBonus.velocidade %></li>
    <li><strong>Agilidade:</strong> <%= atributosBonus.agilidade %></li>
    <li><strong>Nen:</strong> <%= atributosBonus.nen %></li>
</ul>

                <h3>Outros dados</h3>
                <div class="info-group">
                    <p><strong>Tipo de Ratsu:</strong> <%= user.data?.tipoRatsu %></p>
                    <p><strong>Raridade do Nen:</strong> <%= user.data?.raridadeNen %></p>
                    <p><strong>Dinheiro:</strong> <span class="money"><%= user.data?.dinheiro %> J</span></p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
